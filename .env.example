# 抽奖配置示例。复制为 .env 或 .env.local 后修改。
# .env.local 优先级高于 .env

# 抽奖文件所在目录（绝对路径或相对运行目录）。
# 支持多个目录，用逗号分隔，例如：LOTTERY_DIR=./gold,./silver,./bronze
LOTTERY_DIR=./prizes

# 抽奖文件后缀集合，逗号分隔（不含点）。留空表示匹配所有文件。
LOTTERY_EXTENSIONS=jpg,jpeg,png,gif,mp4,pdf

# 一共抽几个奖（从低奖到高奖依次抽取）
AWARD_COUNT=3

# 服务端口
PORT=8787
